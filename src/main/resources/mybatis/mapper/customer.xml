<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cinema.mypage.MypageDAO">
<select id="getCustomerInfo" resultType="com.cinema.mypage.CustomerDTO">
   select id, uid, passwd, realname, nickname, email, region, favorite, mobile, tellecom, created, updated, birthday
   from customer
   where id=#{param1}
</select>

<update id="updateCusInfo" parameterType="com.cinema.mypage.CustomerDTO">
    UPDATE customer
    SET 
        passwd = #{passwd},
        nickname = #{nickname},
        email = #{email},
        region = #{region},
        favorite = #{favorite},
        mobile = #{mobile},
        tellecom = #{tellecom},
        updated = NOW(),
        birthday = #{birthday}
    WHERE id = #{id} 
</update>

<delete id="deleteCustomer">
   delete from customer where id=#{param1}
</delete>

<select id="getInquiryList" resultType="com.cinema.mypage.InquiryDTO">
      select id,customer_id, title, content, answer, created, ancreated, current
      from inquiry
      where customer_id=#{param1}
   </select>

</mapper>