<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cinema.mypage.InquiryDAO">
	<select id="getList" resultType="com.cinema.mypage.InquiryDTO">
		select a.id, a.title, a.created, a.current, b.id as customerId
			from inquiry a
			join customer b on a.id = b.id
	</select>
	
</mapper>